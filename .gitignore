# Environment files (secrets — never commit these)
.env
.env.*
!.env.example

# Runtime databases
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Project data artifacts
data/*.backup
data/*.migrated.backup
